# Local development settings for compose.yaml. Copy to .env, which git ignores.
# These credentials are for a database bound to 127.0.0.1. Never reuse them anywhere else.
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=
POSTGRES_PORT=
